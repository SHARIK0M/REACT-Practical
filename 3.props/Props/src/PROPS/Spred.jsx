import React from 'react'

// only use same name also change upcoming data 
function Spred({name,age}) {
  return (
    <div>
      <h3>{name}</h3>
      <h3>{age}</h3>
    </div>
  )
}

export default Spred
